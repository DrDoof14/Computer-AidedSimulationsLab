The only file you need to run is the "main.py"

run the commands in CMD:

-if you want to check the part related to "FINGERPRINT SET" run:
python main.py 1

-if you want to check the part related to "BIT STRING ARRAY" run:
python main.py 2

-if you want to check the part related to "BLOOM FILTER" run:
python main.py 3